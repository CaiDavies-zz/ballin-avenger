{% block content %}
{% raw %}
<div ng-controller="ViewJob">
    <div ng-repeat="key in text">
    <h1>
        {{ key.title }}
    </h1>
    <p>
      {{ key.location }}
    </p>
  </div>
</div>

{% endraw %}
{% endblock %}
